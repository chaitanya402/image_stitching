# GenAI Carousel Video Generator — runtime dependencies only

# Image processing
pillow>=10.0.0
numpy>=1.24.0

# Video encoding
opencv-python>=4.8.0

# Hugging Face inference (remote SDXL/FLUX via HF Router)
huggingface-hub>=0.23.0      # InferenceClient
requests>=2.31.0

# Optional: HF Inference API free-tier fallback (hf-inference backend)
# No extra packages needed — uses requests above
